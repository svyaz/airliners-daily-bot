meta:
  environment: jvm
  toolchain:
    name: gradle
    version: 21

build:
  args: -Dserver.port=80 -Pproduction
  artifacts:
    build/libs/*.jar: /

run:
  jarName: airliners-daily-bot-boot.jar
  persistenceMount: /data
  containerPort: 80
